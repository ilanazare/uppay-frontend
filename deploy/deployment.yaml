apiVersion: apps/v1
kind: Deployment
metadata:
  name: uppay
  namespace: uppay
spec:
  replicas: 1
  selector:
    matchLabels:
      app: uppay 
  template:
    metadata:
      labels:
        app: uppay 
    spec:
      containers:
      - name: uppay
        image: 039074742130.dkr.ecr.sa-east-1.amazonaws.com/uppay:latest
        ports:
        - containerPort: 80
        volumeMounts:
        - name: nginx-config
          mountPath: /etc/nginx/nginx.conf
          subPath: nginx.conf
      volumes:
      - name: nginx-config
        configMap:
          name: nginx-config